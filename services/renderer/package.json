{
  "name": "az-studio-renderer",
  "version": "1.0.0",
  "private": true,
  "description": "AZ Studio FFmpeg renderer (Cloud Run job): composes timelines into MP4 exports.",
  "type": "module",
  "main": "dist/main.js",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "build": "node build.mjs",
    "typecheck": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@google-cloud/storage": "^8.2.0",
    "firebase-admin": "^14.4.0"
  },
  "devDependencies": {
    "esbuild": "^0.28.2"
  }
}
